# Quiz de revision — 40 questions

## UML (8 questions)

1. Quelle est la difference entre une relation `include` et une relation `extend` dans un diagramme de cas d utilisation ?

2. Dans un diagramme de classes, quelle est la difference entre une composition et une agregation ? Donner un exemple de chaque.

3. Comment representer une association many-to-many entre Etudiant et Cours dans un diagramme de classes ?

4. Quelle cardinalite signifie "un professeur peut enseigner zero ou plusieurs cours, et chaque cours est enseigne par exactement un professeur" ?

5. Quelle est la difference entre un acteur principal et un acteur secondaire dans un diagramme de cas d utilisation ?

6. Peut-on instancier une classe abstraite en UML ? Pourquoi ?

7. Dans quel cas utilise-t-on le stereotype `<<interface>>` dans un diagramme de classes ?

8. Comment represente-t-on l heritage dans un diagramme de classes ?

---

## SQL / T-SQL (12 questions)

9. Quelle est la difference entre INNER JOIN et LEFT JOIN ? Dans quel cas utiliser l un plutot que l autre ?

10. Une requete avec `GROUP BY departement HAVING COUNT(*) > 5` : que retourne-t-elle ?

11. Qu est-ce qu une sous-requete correlee ? Donner un exemple.

12. Pourquoi les requetes preparees protegent-elles contre les injections SQL ?

13. Quelle est la difference entre une vue (VIEW) et une table ?

14. Qu est-ce qu un index clustered vs non-clustered ?

15. Dans une transaction, a quel moment le ROLLBACK est-il declenche automatiquement en T-SQL ?

16. Ecrire la requete qui retourne les clients ayant passe PLUS de commandes que la moyenne.

17. Quelle est la difference entre DELETE et TRUNCATE ?

18. Qu est-ce que le critere ACID dans les transactions ?

19. Comment eviter les deadlocks dans une application avec transactions concurrentes ?

20. Qu est-ce qu une cle etrangere et que se passe-t-il si on essaie de supprimer un enregistrement reference ?

---

## PHP POO (12 questions)

21. Quelle est la difference entre `public`, `protected` et `private` ?

22. Peut-on implementer plusieurs interfaces dans une seule classe PHP ? Et heriter de plusieurs classes ?

23. Qu est-ce que le polymorphisme ? Donner un exemple concret en PHP.

24. A quoi sert le mot-cle `final` sur une classe ? Sur une methode ?

25. Quelle est la difference entre une classe abstraite et une interface ?

26. Qu est-ce que `$this` ? Et `self::` ? Et `parent::` ?

27. Que fait `password_hash()` et pourquoi ne pas utiliser MD5 pour les mots de passe ?

28. Qu est-ce qu une requete preparee PDO ? Ecrire un exemple SELECT avec un parametre.

29. Comment fonctionne la protection CSRF ? Decrire le mecanisme token.

30. Qu est-ce que le pattern Singleton ? Quand l utiliser ?

31. Expliquer le pattern MVC en PHP : quel est le role de chaque composant ?

32. Qu est-ce que `session_regenerate_id(true)` et pourquoi l appeler apres connexion ?

---

## JavaScript (8 questions)

33. Qu est-ce que le hoisting en JavaScript ?

34. Quelle est la difference entre `let`, `const` et `var` ?

35. Expliquer l event loop en JavaScript : call stack, event queue, microtasks.

36. Qu est-ce qu une closure ? Donner un exemple d utilisation pratique.

37. Quelle est la difference entre `Promise.all`, `Promise.allSettled` et `Promise.race` ?

38. Qu est-ce que le debouncing et dans quel cas l utilise-t-on ?

39. Comment proteger une application contre les attaques XSS cote JavaScript ?

40. Expliquer la difference entre l acces synchrone et asynchrone. Pourquoi JavaScript est-il single-threaded mais peut gerer des operations asynchrones ?

---

## Reponses (a completer par l apprenant, pas de correction fournie ici)

Les reponses seront discutees en groupe ou en binome.
L objectif est d etre capable de repondre a l oral en 1 a 2 minutes maximum par question.
