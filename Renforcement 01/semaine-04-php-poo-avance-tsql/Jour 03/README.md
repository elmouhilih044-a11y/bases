# Jour 03 — T-SQL : Procedures stockees et transactions

Note : les exercices utilisent la syntaxe MySQL (DELIMITER, PROCEDURE, TRANSACTION).
Adapter en T-SQL SQL Server si necessaire (CREATE PROCEDURE sans DELIMITER, BEGIN/COMMIT/ROLLBACK).
